[Unit]
Description=Acer PSense service

[Service]
Type=simple
ExecStart=/opt/psense/bin/psense -p /run/psense.pid -c /opt/psense/etc/config.yaml -f
ExecStop=kill -s TERM `cat /run/psense.pid`
PIDFile=/run/psense.pid
TimeoutStopSec=20
Restart=on-failure

[Install]
WantedBy=multi-user.target
